# STUN server URL for WebRTC connection
VITE_STUN_URL=stun:stun.l.google.com:19302

# Logging (client-side)
# Levels: off | error | warn | info | debug
# Default is debug in dev, warn in production
# VITE_LOG_LEVEL=off will silence logs in production
VITE_LOG_LEVEL=debug

# Optional namespace filter (comma-separated). Use * for all.
# Example: VITE_LOG_NS=main,webrtc,assets
# VITE_LOG_NS=*
